---
rust:
  directory: ../../native/wormhole-core
  manifestPath: Cargo.toml

android:
  # arm64 + armv7 for real phones (armv7 = older/32-bit devices), x86_64 for the
  # emulator.
  targets:
    - arm64-v8a
    - armeabi-v7a
    - x86_64
